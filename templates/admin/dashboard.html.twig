{# templates/admin/dashboard.html.twig #}

{# templates/admin/dashboard.html.twig #}

{% extends '@EasyAdmin/layout.html.twig' %}

{% block content %}
    <div class="container mx-auto py-12">
        <h1 class="text-3xl font-bold text-white mb-8">Bienvenue sur votre interface d'administration</h1>
        <p class="text-lg text-gray-400 mb-8">Utilisez les widgets ci-dessous pour accéder aux différentes sections :</p>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            <a href="{{ path('bienImmo_crud') }}" class="block p-6 bg-blue-600 text-white rounded-lg shadow-lg hover:bg-blue-700 transition-transform transform hover:scale-105">
                <div class="flex items-center">
                    <span class="text-4xl mr-4">
                        <i class="fa fa-building"></i>
                    </span>
                    <span class="text-xl font-semibold">Gestion des Biens Immobiliers</span>
                </div>
            </a>

            <a href="{{ path('city_crud') }}" class="block p-6 bg-green-600 text-white rounded-lg shadow-lg hover:bg-green-700 transition-transform transform hover:scale-105">
                <div class="flex items-center">
                    <span class="text-4xl mr-4">
                        <i class="fa fa-city"></i>
                    </span>
                    <span class="text-xl font-semibold">Gestion des Villes</span>
                </div>
            </a>

            {# Ajoutez d'autres widgets ici #}
            <a href="#" class="block p-6 bg-yellow-600 text-white rounded-lg shadow-lg hover:bg-yellow-700 transition-transform transform hover:scale-105">
                <div class="flex items-center">
                    <span class="text-4xl mr-4">
                        <i class="fa fa-cogs"></i>
                    </span>
                    <span class="text-xl font-semibold">Autre Fonctionnalité</span>
                </div>
            </a>
        </div>
    </div>
{% endblock %}
